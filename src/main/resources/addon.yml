name: Pp
version: ${version}${build.number}
main: world.vinecraft.pp.PpAddon
icon: GOLD_BLOCK
api-version: 3.3.3

authors: [tastybento]
description: Prayer plugin

permissions:
    pp.pray:
      description: Allows to use pray command
      default: true
